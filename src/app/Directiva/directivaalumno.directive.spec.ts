/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DirectivaalumnoDirective } from './directivaalumno.directive';

describe('Directive: Directivaalumno', () => {
  it('should create an instance', () => {
    const directive = new DirectivaalumnoDirective();
    expect(directive).toBeTruthy();
  });
});
